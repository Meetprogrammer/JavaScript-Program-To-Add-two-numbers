function addNumbers(num1, num2) {
    return num1 + num2;
  }
  
  let firstNumber = 5;
  let secondNumber = 10;
  let result = addNumbers(firstNumber, secondNumber);
  
  console.log("The result of adding " + firstNumber + " and " + secondNumber + " is " + result);
  